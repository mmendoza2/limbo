<%	loc = ActiveSupport::JSON.decode(open("https://maps.googleapis.com/maps/api/geocode/json?latlng=#{current_user.lat},#{current_user.lng}&result_type=locality&key=AIzaSyCxH3uvobKynTSs63CoAcTQSB0xzCRduCs").read) %>
<div class="navbar navbar-fixed-top " role="navigation" style=" background-color: #f5f5f5;">
  <a id="menu-toggle" href="#" class="btn btn-default" style="margin-top: 2px; margin-left: 4px"><i class="icon-reorder"></i></a>
  <div style="text-align: center; margin-top: -20px; margin-right: 40px">
    <%	 if loc['results'][0].nil? %>

    <%	 else %>
        <%= loc['results'][0]['address_components'][0]['short_name']  %>
    <%	 end %>
  </div>
  <ul class="nav" style=" margin-top: -20px">
    <ul class="pull-right">
      <li id="fat-menu" class="dropdown" style="margin-top: -15px">
        <a href="#" role="button" id="drop3" class="dropdown-toggle" data-toggle="dropdown" >
          <i class="fa fa-3x fa-cog" ></i>
        </a>
        <ul class="dropdown-menu pull-right" role="menu">
          <li role="presentation" ><%= link_to "Profile", current_user, role: "menuitem" %></li>
          <li role="presentation" ><%= link_to "Location", places_path, role: "menuitem" %></li>
          <li><%= link_to "Configuration", edit_user_registration_path(current_user) %></li>
          <li class="divider"></li>
          <li>
            <%= link_to "Log out",  destroy_user_session_path, method: "delete" %>
          </li>
        </ul>
      </li>
    </ul>
  </ul>

</div>
